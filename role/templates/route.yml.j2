apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: {{ app_name }}
  name: {{ app_name }}
  namespace: {{ namespace | string }}
spec:
  port:
    targetPort: 8080-tcp
  to:
    kind: Service
    name: {{ app_name }}
